'use strict';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    image: {
        flex: 1,
        width: null,
        height: null,
        resizeMode: 'stretch'
    }
});

export default styles;